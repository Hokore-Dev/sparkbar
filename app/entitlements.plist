<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Hardened Runtime is required for notarization. SparkBar makes
         outbound HTTPS calls and reads the Claude Code login-keychain item;
         neither needs a special entitlement, so we keep this minimal. -->
    <key>com.apple.security.app-sandbox</key>
    <false/>
</dict>
</plist>
