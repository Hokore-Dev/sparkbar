version: 2
updates:
  # Keep GitHub Actions pinned versions fresh (the app itself has no package deps).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
    labels:
      - "dependencies"
      - "ci"
